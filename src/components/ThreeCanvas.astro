<Fragment>
  <slot />
  <div
    id="animation-root"
    transition:animate="none"
    transition:persist="animation-root"
    class="absolute -z-40 min-h-screen w-full motion-reduce:hidden"
  >
  </div>
  <script>
    import { Halo } from "../vanta/vanta.halo"

    const h = new Halo({
      el: "#animation-root",
      backgroundAlpha: 0,
      mouseControls: false,
      touchControls: false,
      gyroControls: false,
      scale: 1.0,
      minWidth: 200.0,
      minHeight: 200.0,
      amplitudeFactor: 3.0,
      xOffset: 0.5,
      yOffset: 0.5,
      size: 4.0,
      scaleMobile: 1.0,
      // baseColor: 0x43f5,
      baseColor: 0xfcb5c6,
      // baseColor: 0x73cce9,
      // baseColor: 0xff0000,
      color2: 0xff0000,
      backgroundColor: 0x0,
    })

    document.addEventListener("astro:after-swap", h.resize)
  </script>
</Fragment>
