---
import { ViewTransitions } from "astro:transitions";
import Layout from "./Layout.astro";
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout {title}>
  <Fragment slot="head">
    <ViewTransitions />
  </Fragment>
  <slot />
</Layout>
